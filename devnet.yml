# Midnight local development network (self-contained).
# Pinned to the devnet-compatible versions resolved from Docker Hub:
#   node 0.22.5  ·  indexer-standalone 4.2.1  ·  proof-server 8.1.0
# The `dev` preset pre-mints NIGHT to the wallet derived from seed 0x00..01,
# which is the seed this CLI already uses — so no faucet is needed.
# The proof server is NOT managed here — reuse the existing standalone
# midnightntwrk/proof-server:8.1.0 container already running on :6300.
# Start:  docker compose -f devnet.yml up -d
# Stop:   docker compose -f devnet.yml down          (add -v to wipe chain state)
name: midnight-devnet

services:
  node:
    image: midnightntwrk/midnight-node:0.22.5
    container_name: midnight-node
    ports:
      - '9944:9944'
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9944/health']
      interval: 2s
      timeout: 5s
      retries: 20
      start_period: 20s
    environment:
      CFG_PRESET: 'dev'
      SIDECHAIN_BLOCK_BENEFICIARY: '04bcf7ad3be7a5c790460be82a713af570f22e0f801f6659ab8e84a52be6969e'

  indexer:
    image: midnightntwrk/indexer-standalone:4.2.1
    container_name: midnight-indexer
    ports:
      - '8088:8088'
    environment:
      RUST_LOG: 'indexer=info,chain_indexer=info,indexer_api=info,wallet_indexer=info,indexer_common=info,fastrace_opentelemetry=off,info'
      APP__APPLICATION__NETWORK_ID: 'undeployed'
      APP__INFRA__NODE__URL: 'ws://node:9944'
      APP__INFRA__STORAGE__PASSWORD: 'indexer'
      APP__INFRA__PUB_SUB__PASSWORD: 'indexer'
      APP__INFRA__LEDGER_STATE_STORAGE__PASSWORD: 'indexer'
      APP__INFRA__SECRET: '303132333435363738393031323334353637383930313233343536373839303132'
    healthcheck:
      test: ['CMD-SHELL', 'cat /var/run/indexer-standalone/running']
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 10s
    depends_on:
      node:
        condition: service_healthy
